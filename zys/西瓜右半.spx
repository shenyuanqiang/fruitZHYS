onStart => {
	hide
}

onMsg (msg, data) => {
	if msg == "sliceWatermelon" {
		pos := data.([]float64)
		x := pos[0]
		y := pos[1]

		setXYpos x, y
		show

		repeat 10, => {
			changeXpos 15
			changeYpos -8
			turn Right, 30
			wait 0.05
		}
	}

	hide
}
